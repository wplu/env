case "$-" in
    *i*)
        set -o vi
        set filec
        export PS1="\w\n# \u@\h: "
        export PATH="/usr/local/bin:$PATH:/usr/sbin:/sbin"
        #for d in /opt/*/bin; do export PATH="$PATH:$d" ; done
     
    ;; # break

esac
